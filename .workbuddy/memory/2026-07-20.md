# 项目记忆

## 2026-07-20 工作记录

- 发票核销页面(verification.html)三个详情弹窗（发票详情、账单详情、核销金额详情）新增点击置顶功能
- 使用全局 topZIndex 变量管理弹窗层级，每次点击后自动递增 z-index 确保当前弹窗在最顶层
- 修复 verification.html 页面空指针错误（applyInvoiceBtn / .btn-link 元素不存在导致 JS 报错）
- 发票核销列表表格中，在【核销单号】列后新增【发票申请号】列（INVAPPLY202607001 ~ INVAPPLY202607005）
- 新建发票核销轨迹页面 verification-trace.html，参照截图设计稿实现
- 更新所有页面（index.html, invoice.html, invoice-nonstandard.html, verification.html）的侧边栏导航，将【发票核销轨迹】链接指向 verification-trace.html
- 发票核销轨迹页面表格工具栏：导出按钮更名为【导出列表】，并前置新增【导出核销轨迹】按钮
- 发票核销轨迹列表表格中，在【核销状态】列后新增三列：已核销金额、未核销金额、剩余可核销金额
- 将所有页面的【发票核销轨迹】菜单更名为【发票台账】

## 2026-07-21 工作记录

- 对账管理（index.html）筛选条件区域按截图调整：
  - 更新说明文字
  - 标签行：业务（全部/海外仓储/中转代发/转运业务）、账单确认状态（全部/待推送/待确认/待审核/已驳回/已确认/已废弃）
  - 表单字段：编号（客户代码+输入）、单号（账单号+多个空格或逗号隔开）、确认结果、确认方式、处理状态、账单开票状态、来源、销售区域、销售代表、签约主体、客户星级、周期（对账周期+请选择周期）、账期截止时间（日期范围）、服务主体
  - 按钮：查询、重置、收起高级查询
- 发票管理（invoice.html）标准场景开票申请页面的【开票申请】按钮更名为【编辑】，点击跳转到 invoice-edit.html
- 发票管理（invoice-nonstandard.html）非标准场景开票申请页面：按钮区调整为【开票申请】【编辑】【字段管控规则】【推送】【批量驳回】【批量上传发票】【导出申请附件】【导出发票】【导出】；其中【开票申请】点击跳转 invoice-nonstandard-edit.html
- 新建非标准场景发票编辑页面 invoice-nonstandard-edit.html（面包屑：开票申请），参照截图设计稿实现，包含：基本信息表单、关联账单信息、发票抬头信息、发票行信息、底部操作按钮
- 发票编辑页面（invoice-edit.html）功能增强：
  - 开票申请信息区域新增【新增】按钮，点击打开"添加新账单"弹窗（含账单号输入、说明文字）
  - 操作栏【查看】按钮更名为【移除】，点击移除当前行
  - 客户申请金额值可点击，点击打开"修改客户申请金额"弹窗（含客户申请金额/修改后金额/修改后税金）
  - 将【新增】和【移除】按钮放置在同一行（操作列），列名保持"操作"
  - 修改客户申请金额弹窗：移除"修改后税金"字段，将"修改后金额"更名为"修改后含税金额"
  - 开票申请信息表格区域按截图调整：标题提示改为"（注意：若存在账单争议情况请修改客户申请金额）"、数据行更新为截图内容（G2968/深圳市中科技有限公司/深圳易可达科技有限公司/B20260315G29680001/356.36等）、操作列仅保留【移除】按钮、表格底部增加【+】添加行按钮
- 发票管理标准场景（invoice.html）和非标准场景（invoice-nonstandard.html）筛选条件区域按截图调整：
  - 更新顶部说明文字
  - 单据状态/发票状态标签拆分为独立行，发票状态补充"作废中""已作废"
  - 标准场景新增字段：编码（客户代码+输入）、单号（发票申请号+输入）、对账单号、推送状态、开票方式（带信息图标）、来源、客户星级、销售区域、销售代表、创建时间、账单截止日、开票日期、开票场景
  - 非标准场景新增字段：编码（客户代码+输入）、单号（发票申请号+输入）、关联账单号、推送状态、开票日期、操作人、发票类型、操作时间、签约主体、服务主体、开票方式、客户星级、开票场景
  - 操作按钮改为"收起高级查询"
- 发票管理标准场景（invoice.html）列表新增6个字段（本次申请金额（账单金额）、本次申请金额(发票币种)、待开票金额(发票币种)、开票中金额(发票币种)、未开票金额(发票币种)、已开票金额(发票币种)），放在税费金额之后，3行数据已同步填充，表格min-width从1400px扩大到2200px以触发横向滚动条
- 发票管理标准场景（invoice.html）筛选条件【开票场景】改为级联多选下拉框：左列分组（标准场景开票/非标场景开票），右列对应子项（标准→账单开票；非标→国内段跨期开票、非账单开票、预充值开票、汇率差补开票、跨期账单开票），支持复选框多选、标签展示、点击外部关闭
- 发票管理标准场景（invoice.html）级联下拉框样式调整：复选框和筛选值右对齐，筛选值统一固定宽度 110px（以最长值"国内段跨期开票"为基准），实现等距对齐效果
- 发票编辑页面（invoice-edit.html）发票行信息区域按截图及字段要求重构：
  - 按钮区：新增（紫色）、批量导入（紫色）、批量上传附件（文字按钮）、移除（文字按钮）
  - 增加全选行、红色提示文字、第1张发票复选框分组行
  - 表格字段更新为21列：序号、账单号、商品品名、商品品名（本地语）、商品编码、账单币种、待开票金额、剩余可开票金额(含税)、账单币种金额(含税)、开票币种、发票汇率、本次开票金额、本次开票金额(税费)、价税合计、开票币种是否修改、发票汇率是否修改、开票金额是否修改、发票备注、财务BP备注、申请附件、操作
  - 数据行使用下拉框/输入框/只读值，底部增加【+】添加行按钮，合计行改为"发票张数"，表格min-width扩大到2400px
- 发票编辑页面（invoice-edit.html）可核销发票信息列表填充5行测试数据（INV202607001~005，含不同开票场景/币种/金额）
- 发票台账（verification-trace.html）列表在【已核销金额】后新增【核销中金额】列，9行数据已随机填充
- 发票核销页面（verification.html）新增【执行核销任务】按钮及弹窗：
  - 弹窗字段：客户范围（下拉单选：全部客户/指定客户）
  - 选择"指定客户"时显示客户代码和账户编号复选框区域
  - 客户代码测试数据：G1234、G2968、G1345、GAB12、G45CD（G开头，字母+数字，5位以内）
  - 账户编号测试数据：ACG123401、ACG296802、ACG134503、ACGAB1204、ACG45CD05（AC+客户代码+2位随机数）
- 修复 verification.html【执行核销任务】弹窗不显示问题：弹窗 HTML 原位于 </script> 标签之后，导致 JS 执行时 DOM 尚未解析，document.getElementById 返回 null。已将弹窗 HTML 移至 <script> 标签之前。
- 调整【执行核销任务】弹窗内部结构：客户代码、账户编号从"指定客户"时动态显示改为与客户范围同级字段，始终显示；移除不再需要的 `specifyClientArea` 和 `customerScope` change 事件。
- 【执行核销任务】弹窗客户代码和账户编号改为下拉复选框形式：点击触发器展开下拉面板，面板内显示复选框选项，选中后触发器内展示标签，点击标签×可移除，点击外部关闭面板。
- 【执行核销任务】弹窗客户代码和账户编号改为非必填：去掉标签的 `required` 类（红色星号），提交时不再校验至少选择一个客户代码。
- 发票核销页面（verification.html）移除顶部提示文字："说明：发票申请提交后，系统将根据签约主体、客户代码、服务主体等信息自动匹配开票规则。请确保账单信息准确无误后再提交开票申请。标准场景开票支持自动审批，非标准场景需人工审核。"
- 发票核销页面（verification.html）功能增强：
  - 筛选条件新增：核销状态（下拉：核销中/已核销/废弃）、操作人（输入框）、操作时间（日期范围）
  - 表格工具栏新增【批量反核销】按钮
  - 操作列【详情】按钮更名为【反核销】
  - 点击【反核销】或【批量反核销】时弹出确认弹窗："点击后核销金额将自动回滚到对应账单和发票可核销金额中，是否确认对当前核销任务进行反核销？"
