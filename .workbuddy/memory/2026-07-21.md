# 项目记忆

## 2026-07-21
- 为四个页面（index.html、invoice-nonstandard.html、verification.html、verification-trace.html）统一添加操作日志弹窗
- 日志按钮添加 onclick="openLogModal()" 事件绑定
- 弹窗包含字段：操作人、操作时间、操作内容，填充5行测试数据
- 弹窗宽度700px，使用统一 modal-overlay + modal 样式结构
- 支持点击遮罩层关闭
- 修改发票核销页面账单详情弹窗：改为表格形式展示，支持多币种（USD/CNY/EUR 示例），宽度扩大至700px，列：账单币种、账单含税总金额、账单不开票金额、账单可开票总金额
- 修改发票核销页面发票详情弹窗：改为表格形式展示，支持多币种
- 修改发票核销页面核销金额详情弹窗：发票号放入表格第一列，账单详情部分支持多账单
- 移除发票详情和账单详情弹窗，发票号和账单号改为纯文本展示，无点击效果
- 恢复本次核销金额的点击弹窗效果：添加 .verify-trigger 样式（蓝色下划线可点击），绑定 openDetailModal('verifyModalOverlay')
- 修改发票台账（verification-trace.html）：
  - 按钮区新增【是否参与自动核销】按钮（位于【导出列表】前面）
  - 列表表头新增【是否参与自动核销】字段，位于【开票场景】之后
  - 9行数据填充字段值：参与、不参与、参与、无需参与、参与、不参与、无需参与、参与、不参与
  - 操作列新增【是否参与自动核销】修改按钮
- 修复文件编码问题：PowerShell Get-Content 导致 UTF-8 文件损坏（中文变 ??、< 丢失），通过 Python 脚本恢复并修复
- 发票核销列表（verification.html）操作列宽度调整为 240px
- 发票台账（verification-trace.html）操作列宽度由 120px 调整为 240px
- 发票台账操作列按钮折叠：只保留【是否参与自动核销】和【更多】，其他按钮（发票日志、查看账单、发票文件预览、下载发票）折叠到【更多】下拉菜单中，点击展开/收起，点击页面其他区域自动关闭
- 修复发票台账【更多】下拉菜单不显示的问题：将下拉菜单定位方式从 `position: absolute` 改为 `position: fixed`，通过 JS 动态计算位置（基于 toggle 的 `getBoundingClientRect`），避免被 `.table-wrapper` 的 `overflow-x: auto` 裁剪
- 需求规则说明书已生成并发布到飞书云文档：https://ztn.feishu.cn/docx/PoBcdWyDTodEj0xbYE4cdqd6nkd
