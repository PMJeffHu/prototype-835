# 项目记忆

## 2026-07-22
- 新增发票明细页 `invoice-detail.html`：12 列表格（序号、单据申请号、发票申请号、发票号码、开票场景、发票代码、发票总金额、合计税额、票店开票日期、开票日期、最近付款日期、操作）
  - 面包屑：发票管理 > 标准场景开票申请 > 发票明细
  - 2 行测试数据：JPY 104.0000 / GBP 1,777,236.3800
  - 操作列：手动红冲 / 发起红冲 / 更多▼（含查看详情、下载发票、发票日志）/ 作废（禁用）
  - 【更多】下拉使用 `position: fixed` + `getBoundingClientRect` 动态定位
- 修改 `invoice.html`：3 个【发票详情】按钮改为 `<a href="invoice-detail.html">` 链接，指向新建的明细页
- 新增非标场景发票明细页 `invoice-detail-nonstandard.html`：
  - 面包屑：发票管理 > 非标准场景开票申请 > 发票明细
  - 第9列改为"推送开票日期"（区别于标准场景的"票店开票日期"）
  - 1 行测试数据：TESTINV0710 / 非账单开票 / 10.0000 USD
  - 操作列：手动红冲 / 发起红冲 / 作废（禁用）/ 更多▼
- 修改 `invoice-nonstandard.html`：3 个【发票详情】按钮改为 `<a href="invoice-detail-nonstandard.html">` 链接
- 需求规则说明书 v2.0 已更新覆盖飞书云文档：https://ztn.feishu.cn/docx/PoBcdWyDTodEj0xbYE4cdqd6nkd
  - 覆盖 9 个页面（P1~P9），包含新增的标准/非标发票明细页
  - 全部字段以表格形式列出，包括筛选条件、表格列定义、弹窗明细、公共组件
  - 使用 overwrite 命令全文覆盖飞书文档
- 新增发票品名映射页 `product-mapping.html`：
  - 侧边栏第一位菜单（在【对账管理】之前）
  - 筛选条件：销售产品编码（dropdown）、商品品名（input）、商品品名(本地语)、服务主体（dropdown）、所属BU、发票类型（dropdown）
  - 表格 8 列：序号、销售产品编码、*商品品名（必填）、商品品名(本地语)、*服务主体（必填）、所属BU、税率(%)（含 info icon）、发票类型
  - 7 行测试数据（FBU 开头编码 + FBG BU）
  - 底部操作：[编辑]（紫色主按钮）+ [取消]
  - 行点击支持选中高亮（.selected 样式）
  - 下拉框未选时灰色，选中后变深色（.has-value 类）
- 同步更新所有 9 个现有页面的侧边栏：在【对账管理】前插入【发票品名映射】菜单项
- 新增开票流程配置页 `invoice-flow-config.html`：
  - 侧边栏位置：发票品名映射 → 开票流程配置 → 对账管理...
  - 包含两个 Tab：服务主体维度（默认激活）、客户维度
  - **Tab 1 服务主体维度**：筛选含状态 tab（全部/草稿/待审核/启用/停用）、流程编号、服务主体；工具栏 8 个按钮（新增/启用/停用/导出/范围配置/开票场景定义/发票品名映射/品名默认规则）；表格 16 列（含复选框、流程编号、服务主体、发票类型等）；6 行测试数据（ISC 开头编码）；共 56 条
  - **Tab 2 客户维度**：筛选含状态 tab、特殊客户标识 tab（全部/是/否）、客户流程编号、主体流程编号、编码、服务主体、开票规则、客户层级、操作人、创建人、操作时间（4 列网格布局）；工具栏【导出】；表格 15 列；7 行测试数据（ICC/ACG 开头编码）；共 4046 条
  - 操作列含【编辑/操作日志】+【更多】下拉（复制/删除/操作日志），使用 position: fixed 动态定位
  - 状态标签 tab 切换、下拉框颜色变化（.has-value）复用现有交互模式
- 同步更新所有 10 个现有页面（含 product-mapping.html）的侧边栏：在【发票品名映射】后插入【开票流程配置】菜单项
- 需求规则说明书 v3.0 已更新覆盖飞书云文档：https://ztn.feishu.cn/docx/PoBcdWyDTodEj0xbYE4cdqd6nkd
  - 覆盖 11 个页面（P1~P11），新增发票品名映射和开票流程配置（双 Tab）
  - 11 个页面（P1~P11），新增发票品名映射和开票流程配置（双 Tab）
  - 9 个章节，全部字段以表格形式列出
  - 新增编码格式规范（第 8.7 节）：含 ISC/ICC/FBU 等格式定义
  - 使用 overwrite 命令全文覆盖飞书文档
