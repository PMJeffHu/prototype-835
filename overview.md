# 签约业务列表页 - 设计稿转代码

## 完成情况

将用户提供的后台管理系统截图设计稿转化为完整的前端 HTML 页面。

## 页面结构还原

| 区域 | 内容 |
|------|------|
| 侧边栏 | 4 个菜单（对账管理、发票管理、发票核销、发票台账），支持拖拽调整宽度 |
| 顶部提示条 | 灰色背景的业务说明文字 |
| 筛选面板 | 签约业务标签页、单据确认状态标签页、12 个筛选字段（4 列网格布局） |
| 操作栏 | 查询、重置、展开/收起高级查询按钮 |
| 表格工具栏 | 申请开票、导出账单按钮 |
| 数据表格 | 17 列（含复选框、序号、账单号、金额、不开票金额、状态、操作等），支持列宽拖拽 |
| 分页器 | 页码、上下页、每页条数选择 |

## 弹窗功能

- **申请开票弹窗**：点击"申请开票"按钮打开
  - 账单确认表格：9 列（账单号、账单币种、账期、账单金额、账单税费、已开票金额（含税）、开票中金额（含税）、可开票金额（含税）、本次开票金额（含税））
  - 本次开票金额（含税）为文本输入框，支持输入验证（范围：正负 999999999.99）
  - 开票资料表格、发票类型下拉框、备注输入框
- **不开票金额详情弹窗**：点击列表"不开票金额"字段打开
  - 基本信息：账单号、客户代码、账户编号、签约主体、服务主体
  - 金额信息列表：不开票总金额、清账金额、代金券金额、币种
  - 操作记录列表：操作人、操作时间、操作内容
- **修改不开票金额弹窗**：点击列表操作列"修改不开票金额"按钮打开
  - 调整金额（输入框）、调整后不开票金额（文本框，自动计算）、备注（输入框）
  - 底部操作：确定（提交）、返回（关闭）

## 技术实现

- **纯 HTML + CSS + 少量 JS**，无外部依赖，直接浏览器打开即可预览
- 使用 CSS Grid 实现筛选条件 4 列布局
- 使用 Flexbox 实现标签、按钮、分页等行内布局
- 表格横向滚动适配，保证小屏幕也能查看
- 主色调：#5C62D9
- 包含交互：标签页切换、全选/反选、展开收起高级查询、弹窗打开关闭、列宽拖拽、侧边栏拖拽、输入验证、不开票金额详情查看、修改提交

## 待确认/可优化点

- 目前数据为静态 mock 数据，实际接入需替换为接口请求
- 表格列宽基于目测估算，可根据实际数据内容微调
- 更多操作下拉菜单未展开，可后续接入 Popover 组件
